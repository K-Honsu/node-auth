{
  "name": "node-auth",
  "version": "1.0.0",
  "description": "This is a repo containing a new express api for alt grading on auth with api key",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":"nodemon api.js"
  },
  "author": "Feranmi Emmanuel Adeyemi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
